### H4P1 — Rotate String (TCS-type)

- https://leetcode.com/problems/rotate-string/

```python
class Solution:
    def rotateString(self, s: str, goal: str) -> bool:
        if len(s) != len(goal):
            return False
        return goal in (s + s)
```