# 125. Valid Palindrome
**https://leetcode.com/problems/valid-palindrome/submissions/2053317880/**


```class Solution:
    def isPalindrome(self, s: str) -> bool:
        t = ''.join(c.lower() for c in s if c.isalnum())
        return t == t[::-1]
```